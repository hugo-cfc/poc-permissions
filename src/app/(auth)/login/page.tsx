import { LoginForm } from './LoginForm';

export default async function LoginPage() {
  return (
    <section className='w-screen h-screen flex justify-center mt-32'>
      <LoginForm />
    </section>
  );
}
